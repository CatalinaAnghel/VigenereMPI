#pragma once
#include "common.h"

class MatrixHelper {
public:
	/*
	* Initialize the tabula recta
	*/
	void initializeMatrix(char matrix[][SIZE]);

	/*
	* Print a matrix
	*/
	void printMatrix(char matrix[][SIZE]);
};